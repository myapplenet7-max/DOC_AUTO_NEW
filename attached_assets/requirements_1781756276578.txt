fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
easyocr==1.7.1
pdfplumber==0.11.0
pdf2image==1.17.0
numpy==1.26.4
python-docx==1.1.2
Pillow==10.3.0
twilio==9.0.4

# Note: easyocr depends on torch. On a CPU-only deployment (most small
# Replit/VPS instances), install the CPU build explicitly to avoid pulling
# the much larger CUDA build, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# pdf2image also requires the system package "poppler-utils" to be
# installed (apt-get install poppler-utils / pacman -S poppler), since it
# shells out to pdftoppm under the hood.
#
# The first time extract_text() runs with ["en", "te"], EasyOCR will
# download the Telugu model weights (~50-100MB) — make sure the deployment
# environment has outbound network access on first run, or pre-download
# the models into the image/build step.
opencv-python-headless==4.9.0.80
