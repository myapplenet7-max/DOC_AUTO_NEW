- [DocAuto Python Backend](docauto-backend.md) — FastAPI/uvicorn at port 8080; uses PyJWT+bcrypt (not python-jose/passlib, both blocked by package firewall); EasyOCR unavailable (PyTorch too large), pdfplumber handles text PDFs fine.
- [DocAuto Template System](docauto-templates.md) — Template model added; routers/templates.py handles CRUD; services/template_service.py fills placeholders; frontend TemplateLibraryPage added with fill-form, edit, delete, create-new.
- [DocAuto Format Preservation](docauto-format-preservation.md) — mammoth installed for DOCX→HTML; format_service.py does in-place DOCX replacement preserving all formatting; Document model has template_path column; preview-original/template/output endpoints serve HTML via iframe; ComparisonPanel in App.tsx shows 3-panel side-by-side on desktop, tabs on mobile.
- [DocAuto Resume Module](docauto-resume.md) — Resume model+router+service added; python-docx RGBColor is a tuple subclass (use str(accent) for hex, NOT int(accent)); 3 types: fresher/experienced/creative; preview (free watermarked) vs download (1 credit); admin sees all resumes at /api/admin/resumes.
- [DocAuto Pillow Kernel API](docauto-pillow-kernel.md) — Pillow 10+ requires size=(3,3) tuple in ImageFilter.Kernel, NOT integer 3; integer raises 'int object not subscriptable' at runtime.
